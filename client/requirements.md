## Packages
(none needed)

## Notes
Uses existing framer-motion for scroll reveals + staggered entrances
Uses existing shadcn/ui components already in repo (button, card, dialog, toast, tooltip)
Contact form posts to api.contact.create.path with credentials: "include"
SEO is set at runtime via document.title + meta tags in Home page
